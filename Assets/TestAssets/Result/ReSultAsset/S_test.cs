using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class S_test : MonoBehaviour
{
    // Start is called before the first frame update
   
   
    void Start()
    {
        S_Result.SetScore(15, 0, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
